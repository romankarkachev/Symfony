<!DOCTYPE html>

<title>Автор &laquo;{{ author.name }}&raquo;</title>

{% block body %}
    <h1>Автор &laquo;{{ author.name }}&raquo;</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Имя</th>
                <td>{{ author.name }}</td>
            </tr>
            <tr>
                <th>Ссылка на сайт</th>
                <td>{{ author.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('authors_index') }}">&larr; Вернуться в список авторов</a>

    <a href="{{ path('author_edit', {'id': author.id}) }}">изменить</a>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
